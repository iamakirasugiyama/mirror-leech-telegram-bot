name: Manually Deploy to Heroku #1

on: workflow_dispatch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: 17a53256-d1a6-4c7f-8afa-8938751b1902
          heroku_app_name: iamakirasugiyama
          heroku_email: akirasugiyama@yandex.com
          usedocker: true
          docker_heroku_process_type: web
          stack: "container"
          region: "eu"
        env:
          HD_CONFIG_FILE_URL: https://gist.githubusercontent.com/iamakirasugiyama/9ef1a3bc61d4608092e430601164325b/raw/config.env
